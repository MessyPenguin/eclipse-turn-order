/* -----------------------------------------------------------
   AUDIO (simple placeholder)
----------------------------------------------------------- */

function playBeep(type) {
    // Stubbed out for now; you can wire actual sounds later.
    // Keeping it non-blocking and safe to call.
}
/* -----------------------------------------------------------
   SOUND EFFECTS
----------------------------------------------------------- */

function playBeep(type) {
    // Your existing audio logic
}
